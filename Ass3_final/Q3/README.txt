Insmod: the kernel module used to insert the module to the modules of a kernel. 

The code is pretty short and simple we use a task_structure t to store the details we obtain using pid_task, and then we are simply printing the details. Using printk instruction.